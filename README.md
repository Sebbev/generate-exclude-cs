## Features

This addon will generate a settings.json file in the .vscode directory to exclude C# files like `.csproj, .sln, etc..` and it will also exclude directories like `/.vs, /obj and /bin` from the explorer window.

<!-- \!\[feature X\]\(images/feature-x.png\) -->

<!-- > Tip: Many popular extensions utilize animations. This is an excellent way to show off your extension! We recommend short, focused animations that are easy to follow. -->

## Extension Settings

Include if your extension adds any VS Code settings through the `contributes.configuration` extension point.

For example:

This extension contributes the following settings:

- `generate-exclude-cs.generateSettingsFile`: generate the settings.json file in the .vscode directory

## Release Notes

Users appreciate release notes as you update your extension.

### 1.0.0

Initial release.
